# Core application
DB_PATH=./data/health.db
SECRET_KEY=change-this-in-production
ACCESS_TOKEN_EXPIRE_MINUTES=120

# Gradio / server
PORT=7860

# Frontend (React dev)
VITE_API_URL=http://localhost:8000

# AI provider selection: 'local' (default) or 'hf'
AI_PROVIDER=local

# If AI_PROVIDER=hf, provide a free Hugging Face Inference API token:
# Create one at https://huggingface.co/settings/tokens (choose "read" scope)
# Then set one of the following:
HUGGINGFACE_API_TOKEN=
# or
HUGGINGFACEHUB_API_TOKEN=
